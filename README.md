# Hello

## How it works

Open the app and choose a directory. The app will create a file named `.tagger.json` to store information in the chosen directory. After that, you can tag your files within the directory using the app and all the information will be stored in the created file.

Nix

```sh
nix-shell
```

Build

```sh
make
```

Run

```sh
build/main
```
